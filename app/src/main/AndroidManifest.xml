<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".SeQuredApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.SeQuredIdentity"
        android:supportsRtl="true"
        tools:replace="android:allowBackup,android:fullBackupContent,android:dataExtractionRules,android:usesCleartextTraffic"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.SeQuredIdentity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ZXing scanner. Declared explicitly so the manifest merger picks
             our brand-themed activity instead of the library default — the
             library's default uses a light theme that flashes white on launch. -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="fullSensor"
            android:exported="false"
            android:theme="@style/Theme.SeQuredIdentity.PostSplash"
            tools:replace="android:screenOrientation,android:theme" />

        <!-- Android Autofill Framework service. System binds to us when the
             user picks SeQured Identity as their autofill provider. The
             prompt-every-time policy lives in SeQuredAutofillService — every
             fill request bounces through AutofillUnlockActivity for biometric
             + master, so the master never sits in memory between fills. -->
        <service
            android:name=".autofill.SeQuredAutofillService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_AUTOFILL_SERVICE">
            <intent-filter>
                <action android:name="android.service.autofill.AutofillService" />
            </intent-filter>
            <meta-data
                android:name="android.autofill"
                android:resource="@xml/autofill_service" />
        </service>

        <!-- Transparent overlay that handles the biometric + master prompt
             before each fill. NOTE: must NOT use launchMode=singleInstance /
             singleTask or taskAffinity=""  — both break setResult delivery
             back to the autofill framework's PendingIntent. The activity
             must inherit the requesting task so EXTRA_AUTHENTICATION_RESULT
             flows back through the system server. -->
        <activity
            android:name=".autofill.AutofillUnlockActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.SeQuredIdentity.AutofillOverlay" />
    </application>
</manifest>
